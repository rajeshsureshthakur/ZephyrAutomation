SELECT COUNT(*) 
FROM zephyrdata 
WHERE release_id = 15614 
AND FROM_TZ(CAST(last_updated AS TIMESTAMP), DBTIMEZONE) 
    AT TIME ZONE 'UTC' 
    > SYSTIMESTAMP AT TIME ZONE 'UTC' - INTERVAL '10' MINUTE;
